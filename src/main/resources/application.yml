spring:
  application:
    # 应用名
    name: mindMap
  # MongoDB
  data:
    mongodb:
      uri: mongodb://182.92.113.4:27017/map
      password: 1314520
#      username: map

  #  datasource:
#    url: jdbc:mysql://localhost:3306/map?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai
#    username: root
#    password: root
##    # 使用druid数据源
#    type: com.alibaba.druid.pool.DruidDataSource
#    driver-class-name: com.mysql.cj.jdbc.Driver
#    url: mongodb://localhost:27017/map

  servlet:
    multipart:
      enabled: true
  redis:
    database: 0
    port: 6379
    host: 182.92.113.4
    password: 1314520
    lettuce:
      shutdown-timeout: 1000
      pool:
        max-active: 8
        max-wait: -1ms
        max-idle: 8
        min-idle: 0

mybatis:
  mapper-locations: classpath:mapper/**/*.xml
#  type-aliases-package: com.example.mindmap.dao.entity

server:
  # 端口号
  port: 4000

mindMap:
  # 跨域配置
  cors:
    # 允许跨域的域名
    allow-origins:
      - http://182.92.113.4:1024
      - http://182.92.113.4:8080
